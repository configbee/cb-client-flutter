name: configbee_flutter
description: Flutter SDK for Configbee - A feature flags and remote configuration management client library
version: 0.0.1
homepage: https://docs.configbee.com/client-sdks/flutter/
repository: https://github.com/configbee/cb-client-flutter
topics:
  - feature-flags
  - remote-config
  - dynamic-configuration
  - sdk

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_client_sse: ^2.0.0
  http: ^1.2.0
  http_cache_client: ^1.0.5
  http_cache_core: ^1.1.3
  http_cache_hive_store: ^5.1.0
  path_provider: ^2.1.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
